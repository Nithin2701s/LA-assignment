import { MaterialIcons } from "@expo/vector-icons"


const TabIconSymbol = ({name,size,color})=>{

   return (
    <MaterialIcons name={name} size={size} color={color} />
   )
}

export default TabIconSymbol